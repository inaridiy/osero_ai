const size = 8;

export const drawerConst = {
  height: 300,
  width: 300,
  cellSize: (300 / size) * 0.8 * 0.5,
  backgroundColor: "0x008000",
  colorList: { 1: "#000", 2: "#fff" },
};

export const global = {
  size,
};
